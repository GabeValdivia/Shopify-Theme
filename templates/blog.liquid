<h1>{{ blog.title }}</h1>

{% for article in blog.article %}
    <div>
        <a href="{{article.url}}">
            <h2>{{ article.title }}
            <p>{{ article.author }}</p>
            <div>{{ article.excerpt }}</div>
        </a>
    </div>
{% endfor %}